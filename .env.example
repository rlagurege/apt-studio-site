# Database — PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# For local development: postgresql://postgres:postgres@localhost:5432/apt_studio?schema=public
# For production: Use a managed PostgreSQL service (Vercel Postgres, Supabase, Railway, etc.)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?schema=public"

# Staff sign-in (NextAuth)
# 1. NEXTAUTH_SECRET = random string (e.g. openssl rand -base64 32)
# 2. NEXTAUTH_URL = your site URL (e.g. http://localhost:3000 for dev)
# 3. ARTIST_PASSWORDS = JSON object: { "artist-slug": "password", ... } (one entry per artist who can log in)
# 4. STAFF_PASSWORDS = JSON object: { "staff-slug": "password", ... } (for admin/staff like Tami)
NEXTAUTH_SECRET="your-secret-at-least-32-chars"
# For local development:
NEXTAUTH_URL="http://localhost:3000"
# For production (update this after deploying):
# NEXTAUTH_URL="https://addictivepaintattoo.studio"
ARTIST_PASSWORDS='{"big-russ":"416769","kenny-briggs":"changeme","gavin-gomula":"000123","tom-bone":"110244","ron-holt":"666666"}'
STAFF_PASSWORDS='{"tami-gomula":"123456","big-russ":"416769","tom-bone":"110244"}'

# Notion — required for createAppointmentPageInNotion()
# 1. NOTION_SECRET = integration token from https://www.notion.so/my-integrations
# 2. NOTION_DATABASE_ID = from your APT Studio Management database URL (the ID before ?pvs=...)
# 3. In Notion: share that database with your integration (••• → Add connections → your integration)
NOTION_SECRET="secret_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
NOTION_DATABASE_ID="2fbb283044ae80e18349c24c88dfb827"

# OPTIONAL — only if you want email notifications to Tami
# Note: Keep TAMMY_EMAIL variable name for backward compatibility
TAMMY_EMAIL="tami@yourdomain.com"
EMAIL_FROM="APT Studio <no-reply@yourdomain.com>"

SMTP_HOST="smtp.yourprovider.com"
SMTP_PORT="465"
SMTP_USER="no-reply@yourdomain.com"
SMTP_PASS="yourpassword"

# Twilio — for SMS and phone calls (optional)
# Get these from https://console.twilio.com/
TWILIO_ACCOUNT_SID="ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
TWILIO_AUTH_TOKEN="your_auth_token"
TWILIO_PHONE_NUMBER="+1234567890"
